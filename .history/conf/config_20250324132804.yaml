defaults:
  - model: default
  - training: default
  - dataset: default
  - override hydra/job_logging: rich
  - override hydra/hydra_logging: rich
  - _self_

hydra:
  job:
    chdir: true
  job_logging:
    root:
      level: DEBUG