# @package hydra.hydra_logging
version: 1
formatters:
  simple:
    format: "%(message)s"
handlers:
  console:
    class: rich.logging.RichHandler
    formatter: simple
    markup: true
    show_time: true
    show_level: true
    show_path: false
root:
  level: INFO
  handlers: [console]

disable_existing_loggers: false